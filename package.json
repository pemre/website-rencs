{
  "name": "rencs",
  "description": "a task runner for rencs website",
  "version": "1.0.0",
  "engines": {
    "npm": "<=7.0.0",
    "node": "<=12.0.0"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "gulp": "^3.9.1",
    "gulp-clean": "^0.3.2",
    "gulp-concat": "^2.6.1",
    "gulp-html-replace": "^1.6.2",
    "gulp-imagemin": "^3.2.0",
    "gulp-jsonminify": "^1.0.0",
    "gulp-minify-css": "^1.2.4",
    "gulp-minify-html": "^1.0.6",
    "gulp-uglify": "^2.1.2"
  },
  "scripts": {
    "prepare": "gulp",
    "deploy": "npm run prepare && gh-pages -d dist"
  }
}
